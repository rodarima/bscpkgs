#!/bin/sh -ex

>&2 echo "Running launcher"

if [ ! -e "/nix" ]; then
  >&2 echo "missing /nix"
  exit 1
fi

if [ -z "$GARLIC_OUT" ]; then
  >&2 echo "GARLIC_OUT is not set"
  exit 1
fi

mkdir -p "$GARLIC_OUT"
cd "$GARLIC_OUT"

for j in @out@/apps/*; do
  $j/bin/run
done
